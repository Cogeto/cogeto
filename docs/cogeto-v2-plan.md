# Cogeto 2.0: Complete Plan

**Status: BINDING.** This is the plan of record for the 2.0 cycle and supersedes every earlier roadmap. Every confirmed item from the 2.0 discussion is here, assigned to a version, with priority and difficulty. Section 10 is a traceability checklist: if something you asked for is not there, it was missed and must be added. Nothing in this plan is optional-by-omission.

Difficulty: **S** = days · **M** = one to two weeks · **L** = multi-week · **XL** = a month or more.
Priority: **P0** = blocks the version · **P1** = core of the version · **P2** = valuable, can slip one version.

---

## 1. The business driver (decided)

**The wedge: auditable findings over document sets.**

Point Cogeto at a folder of documents; it reads all of them (including scans and spreadsheets), anchors every fact to its subject and its exact span, detects contradictions and supersessions across the whole set, and produces a **signed, printable findings report** an auditor or QA lead can forward. Value arrives in an afternoon, not in weeks.

Why this and not something else: the report is a *forwardable artifact* (chat answers are not), bulk import removes the empty-box problem that kills trials, and contradiction detection with provenance over a document corpus is a job nobody else does honestly. It also uses every asset already built: verification-before-storage, temporal validity, receipts and signing, the eval harness, provenance.

The original professional-memory product remains the base and is not abandoned; the findings wedge is what makes a stranger pay. Everything in 2.0 up to and including V2.3 builds toward one demo sentence: *point it at 400 documents and get a signed findings report.*

---

## 2. Principles introduced in 2.0

1. **Cogeto resolves its own reviews.** Unsupported, partial, and hedged extractions are handled automatically (stored as uncertain, demoted in retrieval, never blocking). There is no manual approval queue for facts. Every automatic suppression or demotion is logged so it stays inspectable and reportable.
2. **Contradictions are surfaced, not queued.** They appear in context (on the source, in the answer, in the report), never as a chore list.
3. **Chat is the door; Sources is the proof.** Things enter through conversation; Sources is where you see and prove what the system knows.
4. **No feature ships without its eval cases and gates.** Published metrics include the unflattering ones.
5. **Boundaries are contracts, not conventions.** Imports, table ownership, job types, and DI tokens all count as boundaries.

---

## 3. V2.0: Clean core and truthful metrics

*Goal: remove what is not working, make the published numbers honest, stop the manual review burden, and prepare the ground so later versions do not land in god files.*

| # | Item | Priority | Difficulty |
|---|---|---|---|
| 3.1 | **Full task removal, backtraced**: **DELIVERED** 2026-07-28 (migration 0035) | P0 | M |
| 3.2 | **Reminders dropped**: **DELIVERED** 2026-07-28 | P0 | S |
| 3.3 | **Automatic review resolution + suppressed-fact log**: **DELIVERED** 2026-07-31 (migration 0039) | P0 | M |
| 3.4 | **Trust-score honesty and eval gates (first wave)**: **DELIVERED** 2026-07-31 (trust-score schema 1.1, no migration) | P0 | M |
| 3.5 | **i18n foundation (en/hr/de/fr, user-level)**: **DELIVERED** 2026-08-02 (no migration) | P1 | M |
| 3.6 | **Targeted modularization**: all four parts **DELIVERED** 2026-08-02 (parts 1, 2 and 4 no migration; part 3 migration 0040) | P1 | L |
| 3.7 | **Correctness and hygiene debts from the audit**: **DELIVERED** 2026-08-02 (no migration) | P1 | S to M |

**3.1 Full task removal.** **DELIVERED 2026-07-28** (migration 0035): open loops are memory-backed, the receipt field stays permanently optional, the passport is at version 2.0. Delete the tasks module (21 files, 3,886 lines), both tables, both enums, 8 endpoints, both prompt families, 5 worker jobs, the reminders CLI, the Tasks page and every tendril (nav badge, dashboard stats, attention kinds, drawer "make this a task", skills accept-as-task, conversation delete preview, API client, query keys), 12 task-pair + 6 derivation-trap + 7 chat eval cases, and the task sections inside the answer and query-rewrite prompts. Five risk points handled deliberately: (a) `tasks_removed` stays as an optional field in the signed receipt `counts_json` forever, so historical receipts still verify; (b) memories carrying `source_type='task_conclusion'` provenance are deleted through the saga before the table goes, since the enum value cannot be dropped and the integrity sweep would flag orphans; (c) the passport manifest's required `tasks` key is a breaking schema version bump under the existing versioning rule; (d) attention, dashboard DTOs, retrieval's static task imports and the chat intent routing order are edited together; (e) demo assertions and the two migrations that enqueue task jobs are cleaned. **Preserved, re-fed from memory:** `commitment`/`open_loop` kinds, the dormant-flag table, the open-loops question path (reads memory rows directly), due-dates from `memory.valid_until`, and the attention surface. No deployed instance exists, so the cut is total: no compatibility shims.

**3.2 Reminders dropped.** **DELIVERED 2026-07-28**. Not rebuilt in 2.0. Due-dates remain visible through attention and Sources. A notification layer may return later as its own feature if partners ask.

**3.3 Automatic review resolution.** **DELIVERED 2026-07-31** (migration 0039). The Review station as a manual queue is gone: the page keeps its route but shows contradictions only, and its nav label, badge, attention line and dashboard tile went with the queue. Unsupported, partial, hedged and unjudgeable outcomes are admitted as `uncertain`, demoted by the existing status multipliers, framed softly in answers, and never blocking, with no human action anywhere in the path.

The single undifferentiated bucket is split into six frozen sub-reasons on `memory.uncertainty_reason`, mapped **totally** from what the verification stage can actually distinguish today, with no default arm: `hedged_in_source`, `partially_supported`, `unsupported`, `unjudgeable`, `structurally_invalid` (the one non-admission case: a blank claim or a blank span) and `legacy_unspecified` (backfill only). A low-confidence-extraction reason was deliberately **not** added, because the extractor emits no confidence signal to justify one. Admission itself is byte-identical to the rule that preceded the taxonomy, so no eval metric moves: labelling split the bucket, it did not move the line.

Every automatic demotion or non-admission writes a `suppressed_fact_log` entry (fact as extracted, source, exact span, sub-reason, verification detail, timestamp, memory id or NULL when withheld), gated exactly as memories are and queryable by source, reason and date range with counts. It is content-bearing, so it joins the deletion saga over every enumerated source and the receipt counts it under `suppressed_facts_removed`; retention is the life of the source. Confirming a fact survives as a contextual action on the memory drawer, producing `user_approved` with its existing precedence. Contradictions were excluded from this path throughout: they are surfaced per principle 2, and their resolution flow is untouched.

**3.4 Trust-score honesty and eval gates, first wave.** **DELIVERED 2026-07-31** (trust-score schema 1.1, no migration). The product publishes its own measured accuracy, and it was publishing the flattering subset.

**Published, having been measured all along:** contradiction **precision**, **supersedes accuracy** with its denominator, and **query-rewrite routing accuracy**, per language and aggregate, in an additive schema 1.1 that leaves all thirteen immutable 1.0 files valid. Supersedes was **0 of 1** before this, a rate over a single case that means nothing whether it passes or fails, so nine pair cases were authored covering the ordinary shapes (explicit update with both intervals, correction with no dates, same-day update ordered by capture time, reverse direction, and a trap that must not be read as supersession). Croatian had **no supersedes coverage at all**, so every Croatian supersedes number published to date was empty rather than good. The failing `en-r008` was neither deleted nor weakened: it still fails, and V2.3 fixes the interval arithmetic behind it. The reconciliation harness gained `supersedes_false_positives`, counted in the denominator, because a supersession acted on a `compatible` pair previously scored as nothing at all.

**Per-language floors** (spec §14.3): a language the harness measures and `gates.json` does not name now fails the gate check. Croatian dedup at 0.833 had sat under a 0.90 aggregate gate for eight releases with nothing failing, because nothing looked.

**Gates recalibrated, none lowered.** Every aggregate floor rose (precision 0.70 to 0.78, recall 0.80 to 0.91, verification 0.75 to 0.86, dedup 0.90 to 0.92, contradiction recall 0.70 to 0.83) and three metrics are gated for the first time. Floors are set at the honest current value, defined as the lowest of six live runs, never at a specification target the project is below. Two decision records: [`docs/eval/gate-model.md`](eval/gate-model.md) (the governing rule, every floor with its target and the work that closes each gap) and [`docs/eval/v1-1-0-precision-drop.md`](eval/v1-1-0-precision-drop.md) (the 3.7-point drop that breached the project's own two-point rule and went unrecorded; cause reconstructed as the corpus expansion, stated as a reconstruction because the per-case data was not retained).

**Query-rewrite cases**, 32 across both languages, asserting the routing decision itself in the router's own order. They immediately found two real Croatian defects, published rather than fixed here: relative dates by month name do not resolve at all (the resolver is English-only `chrono-node`), and a Croatian possessive survives reply-target cleaning (`Aninu` instead of `Ana`).

**Evals on pull requests** (spec §14.2): the golden-set suite (extraction, verification, reconciliation, query-rewrite) runs against committed cached fixtures, keyed on the scoring version, the tier and resolved model, the system prompt verbatim and the full rendered input, so a prompt change misses by construction. A miss fails the job naming the refresh command rather than skipping, and it fails even when application code swallowed the error, which `rewriteQuery` does by design. `--emit-json` is refused in replay mode, so a cached run can never become a published trust score. **The chat suite is deliberately NOT cached**: its answer prompt embeds retrieved facts in retrieval order and equally scored facts come back in a different order every run, so identical model responses still build a different prompt and the cache can never hit. Normalising that out of the key would hide real regressions, so the fix is a stable ordering tiebreak in retrieval fusion, recorded as a follow-up; the chat suite keeps running live post-merge.

**Two findings worth carrying forward.** The reconciliation arm is **not deterministic at `temperature: 0`**: it only looked deterministic because it was measured once per release, and six back-to-back runs move English supersedes accuracy between 25% and 75%. And spec §14.4 also requires **anchoring** and **ambiguity handling** to be gated; neither is, which is what "first wave" means. Two follow-ups, both specified precisely in [`docs/eval/website-follow-up.md`](eval/website-follow-up.md): the retrieval ordering tiebreak that would let the chat suite join the cached gate, and the website change (the trust page renders a hardcoded metric list, so it accepts the 1.1 files without breaking and shows the new metrics only after that change).

**3.5 i18n foundation.** **DELIVERED 2026-08-02** (no migration). User-level language for **English, Croatian, German and French** (French added to the plan's en/hr/de at the owner's request), English as the default and the fallback for every missing key. Frontend: react-i18next over i18next, **906 keys** across 21 SPA namespaces plus 3 server namespaces, one file per product surface so a translator opens a small coherent file. Keys are structural (`sources.detail.emptyState.title`), interpolation is named, plurals go through CLDR suffixes (Croatian's one/few/other, French's one/many/other), and no sentence is assembled from fragments. `npm run i18n:check` runs inside the required `lint` check and fails the build on a missing or orphaned key, a missing plural category, a dropped `{{placeholder}}`, an em dash in an English source value, an unused key, or a reintroduced hardcoded literal (a documented heuristic, honest about what it does not catch). `npm run i18n:add -- <locale>` scaffolds a new language in one command. Backend: the files with inline en/hr strings (dreaming digest, deterministic chat replies, research thread message) moved behind a small server catalogue with the same key discipline; log lines, developer errors and prompt assembly stayed put, because they are not copy. System-generated content still follows the per-user preferred-language rule, replies still mirror, strict mode is unchanged. Date, time, number and file-size formatting now follows the active interface locale through one helper, fixing the hardcoded `en-GB` site the audit found and the dozen call sites that silently followed the browser instead of the user. **Stated honestly and prominently:** UI language support is not extraction-quality support. Only English and Croatian have a golden corpus and per-language gates; German and French are interface languages until corpora exist for them, and the settings page, [`docs/features/i18n.md`](features/i18n.md) and [`docs/eval/gate-model.md`](eval/gate-model.md) all say so in the same words. **Authoring the hr/de/fr translations is deliberately a separate task**: every scaffold carries the English text as its value, so an untranslated locale renders correctly today.

**3.6 Targeted modularization.** Not a full rewrite: the split the rest of 2.0 depends on. Define the **boundary contract** (imports, table ownership, job-type contracts, DI tokens) and close the enforcement gaps the audit found: barrel laundering of live Drizzle tables, the missing `passport` entry in the dependency rules, the global-module policy (7 of 11 today), and raw SQL reaching across modules from the composition root. Split the two god surfaces: **`connectors/`** (7.9k lines, six unrelated families) into its families, moving chat-the-connector out of retrieval where it structurally belongs; and **`chat.service.ts`** (1,154 lines with positional-order-load-bearing optional DI) into an orchestrator with explicit intent handlers, because chat becomes the capture surface in V2.2 and will only grow. Convert `source_type` from a hard Postgres enum into a **registry**, since every new reader and connector otherwise costs a memory-owned migration plus a hardcoded-switch tax across six files. Dissolve the accidental thirteenth context in `entrypoints/`.

**Part 1, boundary enforcement: DELIVERED 2026-08-02** (no migration, no behaviour change). Enforcement first, layout later: the tooling was reporting **zero violations over boundaries that were largely unenforced**, which is a claim rather than a check. The contract is now written down in [`module-boundary-contract.md`](module-boundary-contract.md), naming the owner of all **37 tables**, **15 job types** and **27 DI tokens**, plus the policy for when a module may be global, and it is machine-enforced: the `boundaries` check gained a rule that **a barrel may not re-export a live table** (spec §15.2, type-only exports still allowed) and the `passport` context, absent from the rule set since it was written and therefore unchecked rather than clean; a new `boundary-contract.spec.ts` in the required `test` check verifies table ownership including raw SQL, job-type ownership and worker-registry completeness, token ownership, and the global-module allowlist, and fails if the dependency rules and the module directories ever disagree again. `infrastructure/index.ts` **stopped re-exporting its ten live Drizzle tables**; the eight modules that read them go through two new narrow functions (`jobRunState`, `readAuditEntries`), which also collapsed five byte-identical copies of the same queue probe into one. Two of the eleven global modules (`UserContextModule`, `ChatSourceModule`) were un-globaled and the six implicit dependencies that surfaced became explicit imports. **Seven violations fixed, fourteen recorded** in the contract, each with the part that closes it: the remaining nine globals, the raw SQL in `entrypoints/` and the dev tooling, and the `source_type` enum. Parts 2 to 4 (entrypoints dissolution, the connectors split and the `source_type` registry, the chat split) follow as their own pull requests.

**Part 2, the entrypoints dissolution: DELIVERED 2026-08-02** (no migration, no behaviour change, no route change). `entrypoints/` had become the accidental thirteenth context: **seven production controllers**, two large services, and raw SQL spanning six modules' tables. It now holds composition roots, root wiring, CLIs and repo-invariant specs, and nothing else, with that list written into its README as a rule rather than a description. Two surfaces genuinely span several contexts and became **declared contexts**: **`attention`** (the feed and the dashboard statistics, which composes memory, retrieval, agents and ingestion) took ownership of `attention_state` and `attention_dismissal` outright, and **`operations`** (the health report, the capability registry, queue administration, the audit browse) owns no tables and reads everything through the owning modules' public interfaces. Three controllers had exactly one owner and went to it: `/api/settings/model-config` to `model-gateway`, `/api/config` + `/api/config/demo-login` to `identity`, `/api/instance/public-key` to `memory`. Infrastructure gained the narrow readers that replaced the raw SQL (`readAuditPage`, five queue-administration readers plus `retryDeadLetter`, and `InstanceProbes`, which keeps the health report's dedicated two-connection pool so a saturated application pool cannot make it hang). Every moved surface stopped injecting the composition root's `CogetoConfig` and declares the fields it reads instead. **The dependency-cruiser table-ownership allowlist is now empty** and no spec is allowlisted either; six recorded exceptions remain, all for parts 3 and 4. Two records were corrected rather than quietly satisfied: `MemoryModule`'s globality moved to part 3 (it is the same composition rewrite as `ConnectorsModule`), and `erase-task-conclusions.ts` was **not** deleted, because migration 0035 still refuses to run without it and the operator runbook documents it as the 1.x upgrade step. **All 94 routes register identically**, verified by diffing the boot-time route list before and after.

**3.7 Correctness and hygiene debts.** **DELIVERED 2026-08-02** (no migration, no schema change). The tail of the audit list: audit-log **read events** including passport export, file downloads and model-gateway egress; the instance-wide **receipts-verify** endpoint; the `MemoryStore` ungated system methods behind a worker-only construction rather than convention; scope on chat capture; dev-seed and demo code out of the production image; the stale CLAUDE.md; and the cheap half of the duplication list.

**What the security waves had already covered, verified before anything was changed.** The **passport export lifecycle** is audited (`export_requested`, `export_ready`, `export_downloaded`, `export_expired`, wave 4, SEC-9). `GET /api/integrity` is admin-gated (wave 1, SEC-6). **Source maps** are gone from the shipped artifact (wave 4, SEC-32) and the four dev/demo entrypoints are `rm`-ed from the runtime image; the `project/demo` library stays deliberately, because the worker's scheduled reset imports it, and `erase-task-conclusions` stays because migration 0035 refuses to run without it (recorded in item 3.6 part 2, not an oversight). **Web date formatting** was centralised by item 3.5, so there was nothing left to consolidate. **Dead files** were already gone: a no-importer sweep over the whole tree returns only CLIs. CLAUDE.md was **not** "badly stale": every path in it resolved, and it was one item behind (3.6 was missing entirely).

**Audit-log read coverage.** A presigned **file download** now writes `file.downloaded`, the moment a stored original becomes reachable outside the instance and the one egress path in the product that recorded nothing. **Model-gateway egress** writes one entry per call through the seam: tier, provider, resolved model, whether redaction was in the chain, character and token counts, latency, and success. Structural only, asserted by a test that greps every word of a planted prompt and completion out of the serialised entries; a failure to write is logged and never fails the call, which has already happened and already egressed. Volume is bounded by the per-user daily budget wave 4 made durable; retention and export for the trail are V2.4 item 7.4, which is also where the rest of the read coverage lands. **Org stamping** is complete: 33 of 40 writers already stamped, the four that carry an owner but ran in the worker with no Principal now resolve the org through the identity directory (the mechanism its own comment already described), and the three genuinely instance-wide writers say in the code why they are NULL rather than looking unstamped.

**Receipts-verify.** Trimmed, not blocked. The chain WALK stays instance-wide, because a per-user subset of a hash chain verifies nothing, but an ordinary caller gets the verdict plus **their own** confirmed and pending counts and no error string; an administrator gets the instance-wide report. The pattern is the health report's from SEC-3, and it keeps the "chain verified" pill on the user's own Forgotten ledger, which is the guarantee the product sells. One correction to this plan's earlier wording: the endpoint never returned source ids, only counts and a receipt id in the first-error string, which is why the audit rated it LOW.

**The ungated system methods.** Nine, on a class every request-path module injects, held back by a section comment, and one of them, `listBySourceSystem`, was genuinely being called on `GET /api/research/runs/:id/progress`. Two had no caller at all and are deleted. The request-path one became `countBySourceForPrincipal`, gated like every other read and returning the identical number. The remaining six moved to **`MemorySystemStore`, provided only when a root registers memory with `systemReads: true`, which only the worker root does**: in the app process the provider does not exist, so a service that asked for it would fail to resolve at boot rather than read across every owner at runtime. The skill engine, which lives in both roots because the app approves a plan and the worker advances it, receives it through its named-options bag and asserts presence on the advance path, so the app process never holds a reference. `syncStatusPayloads` deliberately stayed on `MemoryStore`: it reads no content and writes the Qdrant status payload of ids a gated write path just transitioned.

**Chat capture scope.** `ChatSourceReader` omitted `scope`, so the embed-store stage's `?? 'private'` decided it and a user whose default capture scope is `shared` got shared memories from notes, files, email and web and private ones from chat. It now stamps the owner's default explicitly, resolved the way the email intake resolves it. The wiring needed a slim `SettingsPortsModule`, because the full settings module imports memory and memory imports the chat ports, which would have been a cycle: the same slim-ports shape item 3.6 part 4 introduced for exactly this reason.

**Consolidation, mechanical only, and what was skipped.** Done: the **Zod-to-400 adapter**, spelled out identically in eighteen controllers, is `parseOrBadRequest` with the same joiner, order and exception, so no response body changes (the five controllers that answer with a fixed message are left alone, because they answer differently); the **research citation-marker grammar** is one definition in `@cogeto/shared` with two exported forms, capturing for the resolvers and whole-token for `split`, where inner groups would leak into the result; the **scope-and-sensitive gate** is one SQL expression parameterised by columns, serving `memory` and `suppressed_fact_log`, with the Qdrant twin left separate on purpose since it is the same rule in a different language kept in parity by test; and **236 barrel exports referenced nowhere outside their own module** are gone, narrowing every module's public interface (the interval predicate is kept exported even though unused, because spec §3's "it exists once" is only enforceable if the one definition is the reachable one). Skipped, with reasons: the **three eval-harness skeletons**, whose consolidation is a redesign of the harness rather than an extraction; the **twenty hand-written owner gates**, which are one `eq(table.ownerId, principal.userId)` each over twelve different tables and are not duplication in any consolidatable sense; and the **user-context preamble idioms**, already down to one `buildContextBlock` with three thin call-site wrappers left.

---

## 4. V2.1: Read everything, anchored

*Goal: no document arrives that Cogeto cannot read, and every fact knows what it is about.*

| # | Item | Priority | Difficulty |
|---|---|---|---|
| 4.1 | **Reading layer: spreadsheets, scans, vision** (DELIVERED 2026-08-03) | P0 | L |
| 4.2 | **Source-context anchoring** (DELIVERED 2026-08-04) | P0 | M |
| 4.3 | **Per-source extraction gate** (DELIVERED 2026-08-04) | P0 | M |

**4.1 Reading layer, native-format half: DELIVERED 2026-08-03** (migration 0041, `feature/reading-layer-native`). **XLSX and CSV are read**, and a format is now a **registered reader rather than a branch in a switch**, which is the part the OCR and vision tiers plug into next.

The seam (`project/src/files/reading/`) selects by **magic bytes, with the declared type and the extension as hints**: `PK` was never enough once workbooks existed, so the sniff walks the ZIP central directory for `word/document.xml` or `xl/workbook.xml`, and a pre-2007 OLE2 file is refused by name rather than as a generic failure. A mislabelled upload is routed by what it is or refused, never trusted. PDF and DOCX moved behind the seam **unchanged and byte-identical**, proved against the pre-seam implementation copied verbatim into the spec, because the golden set is scored on that text and the eval cache is keyed on it.

Provenance is a **structured locator** now, not a string: page number, paragraph index, or sheet name plus row plus A1 cell range plus the columns involved. Nothing downstream consumes it yet, and that is deliberate: 5.2 and 6.x render it, and defining the shape after those surfaces exist would mean parsing a string back into what the reader already knew. A span that cannot be found resolves to nothing rather than to a guess.

Spreadsheets are read as **statements, not grids**: every row carries its column context on every row (chunking strands headers), the header is FOUND under title blocks and merged banners rather than assumed, merged cells resolve to their master, decorative and empty rows are skipped without shifting row numbers, and formulas contribute their **computed value**, never their text, with an uncached or errored cell left out and recorded by reference instead of guessed. CSV detects delimiter and encoding with a documented fallback (BOM, then strict UTF-8, then `windows-1250`, because Croatian is the non-English corpus language and the choice only touches bytes at or above 0x80).

Row caps are per sheet and per file, configurable, and **truncation is recorded on the source and shown in the drawer**, never written into the text where the extractor would remember it as a fact. That report (`file_read_report`, owned by `files`, in the deletion cascade) also separates **`unsupported_format` from `read_failed`**, which are different facts about the world, and labels a file that yielded nothing as `empty`/`no_text`: the honest label that the OCR half replaces with recovered text.

Ten golden cases, five scenarios in English and Croatian, each holding the real workbook or CSV plus the reader options it is read under, with a spec pinning the corpus text to the reader so the two cannot drift.

**4.1 Reading layer, OCR and vision half: DELIVERED 2026-08-03** (`feature/reading-layer-vision`). A page that is a picture is now read, and a page that cannot be read says so.

The ladder is **deterministic, cheapest-first, decided per page, and spends no model call on deciding**: the page's own text layer when it is USABLE (scored, because a scan usually carries a token layer of ligature soup and treating presence as usability is how a scan reports itself as read), then local Tesseract with English, Croatian and German data, then a vision model for what OCR could not read and for pages that are pictures rather than text. Two thresholds were measured rather than guessed, and the ink one was measured because the first value was wrong: a blank page reads 0.000 and one line of text reads 0.010, so the 2% picture threshold reasoned from "a diagram covers whole percents" would have discarded pages with content on them. It is 0.3%.

**Vision is a PROBED capability, never a configuration flag.** A GGUF model is multimodal only when its multimodal projector is loaded beside the weights, the same weights serve happily as a text model, and neither the model name nor `ollama list` shows the difference, so the probe sends a real image at boot and on the registry's schedule. Its failures are four distinguishable reasons, and `image_rejected` names the projector on a local runtime because that is where the problem is and the last place an operator looks. Redaction and vision are mutually exclusive and fail closed: pixels cannot be pseudonymized.

Recovered text enters the pipeline unchanged. The TIER is recorded on the provenance locator, because a fact transcribed from a photograph is weaker evidence than one lifted from a text layer; facts are otherwise statused normally, since marking every fact from every scan `uncertain` would empty that status of the meaning it has. Caps bound the cost by construction (20 pages per document, 100 per user per day), and hitting one stops escalation and marks the rest honestly rather than failing the file.

**No silent emptiness.** `needs_vision` is a state about the INSTANCE, not the document, so the retained bytes are re-readable: a reprocess action per source and a list of everything awaiting a capability. Making a re-read reconcile rather than duplicate required a real fix, and it was a latent defect this feature exposed: reconciliation excluded candidates from the same SOURCE, which was identical to "the same run" only because a source was ingested exactly once. The pipeline now excludes the same BATCH and dreaming keeps the source rule.

**Not delivered, stated plainly:** the vision path is not gated in CI, because gating it needs a vision model in CI and there is none. The deterministic ladder is fully covered by the `test` check; the vision prompt's own quality is exercised by fixtures an operator runs against their own runtime.

**4.1 Reading layer.** Extend beyond PDF and DOCX text to **XLSX and CSV** (sheets and tables flattened into extractable statements) and to **scanned or image-only PDFs**, which today pass silently as "done, zero facts". Scanned pages go first through **local OCR** (Tesseract-class, CPU-only, English and Croatian language packs, in-instance, nothing leaves the box). When the instance runs a **local vision model** via the local runtime, hard cases (poor scans, handwriting, tables, simple diagrams) are read by the vision tier through the model gateway. Recovered text enters the existing pipeline unchanged: extraction, independent verification against the recovered span, statuses, provenance to the file and page. A file that still yields nothing readable is **honestly labelled** ("scanned document, no readable text") in the source drawer rather than shown as processed: no silent emptiness, no fabricated facts. Ships with golden cases per format and language; the vision path is eval-gated like every other model task.

**4.2 Source-context anchoring: DELIVERED 2026-08-04** (migration 0043, `feature/source-context-anchoring`, prompts `anchoring/v0001` + `extraction/v0005`). One cheap pipeline-tier call over the document opening plus filename produces the source context (subjects, document class, revision, each confident or uncertain), stored on `source_context` (ingestion-owned, content-bearing, in the deletion cascade) and injected into every chunk's extraction call as a FENCED `DOCUMENT CONTEXT` block, fenced because the context is the document's own words and an unfenced block would be a laundered path around the fence. Subject resolution: the fact's own text wins, then the nearest section heading, then the single confident document subject, then null exactly as before; an empty or failed context renders a byte-identical pre-anchoring input (spec 1.5.2 is structural). Editable on the source drawer; a user edit is authoritative and re-anchoring is the existing reprocess action, superseding through the reconcile stage's same-batch exclusion. Gated by the plan-named cases (`en-a001`/`hr-a001` per-section subjects under the zero-tolerance subject gate; `en-r015`/`hr-r014` and `en-r016`/`hr-r015` datasheet pairs) with the fixture refresh recorded Mistral-routed. Full narrative: [`docs/features/anchoring.md`](features/anchoring.md).

*As originally planned:* At the read stage, before chunking, one cheap model call over the document's opening (first pages, title block, headers) plus its filename produces a **source context**: subject entities (product models, project names, parties), document class (datasheet, spec, manual, contract), and revision, each marked confident or uncertain. Stored on the source row and injected into every chunk's extraction call, so a chunk saying only "Device has one antenna" extracts as a fact about model AAA rather than about "device". Where a document covers several models, section headings carry the per-section subject and the extractor prefers the nearest explicit subject over the document default. Multi-value and low-confidence cases **fall back to current behavior** (generic entity) rather than guessing, so anchoring can only reduce ambiguity, never invent it. The context is visible on the source detail and **editable**; correcting it re-anchors that source's facts as supersessions. Golden cases gate it: two same-boilerplate datasheets for different models must neither merge nor flag as contradiction, and one multi-model datasheet must anchor per section.

**4.3 Per-source extraction gate: DELIVERED 2026-08-04** (migration 0042, `feature/extraction-gate`). Extraction is admission controlled per source and per connector (spec 1.6): per source kind, an owner can disable extraction, set a fact budget (joins the min with the parse cap and the registry budget), set retention in days (stamps `valid_until` only on facts without their own validity; the dreaming staleness pass handles the lapse), and add allow/deny rules: `document_class` bound to the reading layer's detected format, `source_id` deny-only, `channel`/`folder` reserved schema-side for connectors and bulk import. Enforced at one chokepoint in the pipeline before any model call; refusals land in a metadata-only ledger (the `email_refusal` precedent) with a 30-day prune and deletion-cascade coverage, so a gated source never looks processed-with-zero-facts. An absent gate row is byte-identical to prior behaviour. Full narrative: [`docs/features/extraction-gate.md`](features/extraction-gate.md). One correction to the paragraph as originally planned: "prompt-injection defence, which the extraction prompt lacks today" was already false when 4.3 started: the SEC-4 wave landed the untrusted fence plus the forged-framing and metadata-label guards in the extraction path, so 4.3 changed no prompt and the eval cache was untouched.

*As originally planned:* the analogue of the first-person rule, for extraction rather than tasks, and the prerequisite for bulk import and observed connectors. Per-source and per-connector controls: enable/disable extraction, fact budgets, retention, and admission rules, generalizing the email allowlist precedent (which channels, which folders, which document classes). Without it, one bad folder floods the corpus at full model cost.

---

## 5. V2.2 Sources: capture, proof, and volume

*Goal: chat is the only door; Sources is the audit surface; a trial starts full instead of empty.*

| # | Item | Priority | Difficulty |
|---|---|---|---|
| 5.1 | **Chat-centric capture**: **DELIVERED** 2026-08-06 (migration 0045) | P0 | M |
| 5.2 | **Sources: three-level redesign**: **DELIVERED** 2026-08-06 (migration 0046) | P0 | L |
| 5.3 | **Bulk import + revision linking**: **DELIVERED** 2026-08-06 (migration 0047) | P1 | L |

**5.1 Chat-centric capture.** **DELIVERED 2026-08-06** (migration 0045, `feature/chat-centric-capture`, prompt `answer/v0008`). Chat is the conversational door; Sources is the deliberate one; the Memories tab stopped being an input surface. Files attach in chat via a **paperclip** with the same validation, byte cap, rate bucket and daily quota as the upload endpoint. The default is ingestion through the ONE existing path (the chat endpoint delegates to the files module's upload, so a chat attachment is an ordinary file source with facts, provenance, read report, anchoring and gate treatment); a chat-owned `chat_attachment` row links the source to its conversation and message, and the conversation renders a first-class, persistent card: honest progress (reading, extracting, verifying, storing) from a new metadata-only `ingestion_progress` row the pipeline upserts outside the job transaction, then the real numbers on settle ("added to sources: 47 facts, 1 contradiction", counted per source through the gated stores and stamped onto the row), a link to the source on Sources, the extraction gate's refusal stated instead of a fact count that hides it, and the reading layer's reason when a file could not be read. The **"don't remember this file"** toggle keeps a file transient and conversation-only, with the storage terms decided and stated: the bytes are staged at the discard-mode staging twin, read ONCE by a chat-owned worker job through the same reading ladder (OCR and vision included, same caps and spend metering), and deleted commit-then-delete with the 15-minute backstop; the extracted text lives on the chat-owned row for the life of the conversation, is injected into the answer path as a FENCED `ATTACHED FILES` block (`answer/v0008`, attribution in words, never `[F#]` and never `[U]`, the user-context precedent; absent attachments the input is byte-identical, so the eval cases render unchanged), is erased by the conversation deletion saga and counted on its receipt (`chat_attachments_removed`), and never becomes a source, a fact, or visible to another conversation. Notes are captured only through the existing "Remember this" in chat; the Memories tab keeps its governance surfaces and gained a dismissible pointer to where capture now lives. Single upload moved to the new **Sources** page (nav section, route, the moved upload card with its settle-on-read-report rule, a deep link into the source drawer), placed so bulk import (5.3) joins it there; the flat source list itself is 5.2. Every prior entry path survives: chat capture, chat attachment, deliberate upload, email intake, web research capture, and the API endpoints, unchanged.

**5.2 Sources, three levels.** **DELIVERED 2026-08-06** (migration 0046, `feature/sources-redesign`). The Memories tab became **Sources**, read, audit and resolve. A new declared composition context (`sources/`, the `attention`/`operations` shape: no tables, every read through the owning modules' public interfaces) serves the two new endpoints: `GET /api/source-catalog` (level one: one row per source of every type, k-way merged from per-family cursor listings plus memory's distinct-provenance enumeration for chat captures and discarded originals, with badges computed per PAGE in one grouped query per family and badge FILTERS driven by their own indexed queries) and `GET /api/source-catalog/:type/:id` (level two: facts joined to their verification evidence, the suppressed-fact log, the contradictions with resolution state, the anchoring context and the gate refusal, in one round trip, owner-only with structural ownership resolution). The reader seam's locators finally have their consumer: **span locators persist at admission** (`verification_result.span_locators` + `suppressed_fact_log.span_locators`, located by the shared `locateSpan` where the segments are still in hand, because a discard-mode original can never be re-read), the source detail renders the located text per format (page and tier, paragraph, sheet and A1 range), and pre-locator facts honestly read "no location recorded" with reprocess as the remedy. Fact detail is the evolved memory drawer: hedge phrase, located span, every contradiction relation with its counterpart one click away, and **which answers cited it**, read from the stored canonical `{{cite:<id>}}` tokens (the linkage was already stored as content; redaction erases it with the memory, so history is honest and no new table exists). The old flat list is demoted to a **filtered fact search** on /memories (status, uncertainty sub-reason, entity, content, and a changed-since mode over the change feed's new HTTP route), reachable from Sources rather than the rail; every legacy deep link (`?open=`, `?status=`, citation chips, attention hrefs, digest lines) resolves unchanged. The original numbered rows below stand as delivered:
- **Sources list (default view):** one row per document, note, or email, with name, date, fact count, and status badges (contradictions, superseded, suppressed).
- **Source detail:** every extracted fact with its status and **exact source span**, plus the anchoring context (editable, per 4.2) and the suppressed-fact log for that source.
- **Fact detail:** full lifecycle: extraction, verification result and its span, supersession chain, and which answers cited it.
The old flat all-memories list is demoted to a **filtered search view** across facts (contradicted, changed since a date, content search).

**5.3 Bulk import + revision linking.** **DELIVERED 2026-08-06** (migration 0047, `feature/bulk-import`). Point Cogeto at a folder, a ZIP archive, or an S3-style path: **manifest first** (names, sizes, content hashes; a ZIP is enumerated without a dependency by walking its central directory; S3 credentials are used for the listing and the staging copy and are **never stored**), exclusions decided on the manifest, and **nothing ingested until confirmed**. The two hash cases stay distinct: content already stored is a **duplicate** (skipped, counted); the same normalized filename with a different hash is a **revision candidate** (ingested normally, nominated for the linker). A new `imports/` module owns `import_run` + `import_item` and a worker-side coordinator that feeds every document through the ONE existing upload path at **demoted queue priority 100** with an **in-flight cap of 1** (`COGETO_IMPORT_IN_FLIGHT`, worker concurrency is 2, so an interactive job always finds a slot), so an import cannot starve the instance; the extraction gate and the per-user daily caps apply exactly as for a single upload, and a cap-exhausted import **pauses visibly** (30-minute re-check) instead of bypassing. Resumable by construction (all state is rows; a restart re-runs the pass), per-file failure isolation with named reasons, honest cancellation (in-flight items settle and are reported, staging is cleaned, nothing pretends). Revision linking follows the frozen decision record [`docs/features/revisions.md`](features/revisions.md): S1 (comparable anchored revision fields from 4.2, newer is later) links `auto` at high confidence; S2 (subject overlap >= 0.5 AND class match AND 8-token shingle similarity >= 0.6) proposes at medium; below the bar **nothing is recorded**, because a wrong link corrupts the findings story while a missed link is just today's behaviour. `source_revision` (ingestion-owned, in the deletion cascade) carries the supersedes-source pointer with basis and confidence; facts get **nothing new**, existing reconciliation remains the only fact-level mechanism. Proposals are confirmed or rejected on the source drawer, a **rejected pair is never re-proposed** (its row stands as the memory of that decision), and a manual link overrides even a rejection; all three are audited. The import is a first-class Principal-gated record: erasing an ingested source **tombstones** its item (name cleared, arithmetic kept, so a provable deletion leaves no orphaned filename while the record's counts stay honest), and the completion summary's numbers are all real, computed from the owning stores at finalize, each clickable to its evidence on Sources. That closing summary is the sales demo.

---

## 6. V2.3 Findings: contradictions that hold, and the artifact

*Goal: the thing a QA lead forwards. This version is the wedge.*

| # | Item | Priority | Difficulty |
|---|---|---|---|
| 6.1 | **Contradiction coverage overhaul** | P0 | L |
| 6.2 | **Report generator** | P0 | M |
| 6.3 | **Ambiguity detection and fan-out answers** | P1 | M |
| 6.4 | **Eval gates, second wave (vertical corpus)** | P1 | M |

**6.1 Contradiction coverage overhaul.** **DELIVERED 2026-08-07** (migration 0048, `feature/contradiction-coverage`, prompt `reconcile_contradiction/v0002`, reconcile config v2, decision record [`docs/features/findings.md`](features/findings.md)). Entity matching went from byte-equality to folding (case, punctuation, diacritics, legal suffixes) plus the owner's growable `entity_alias` set (the cross-language mechanism; Settings surface, `/api/reconcile-aliases`) plus a narrow mid-token typo rule, with the alias-expanded subject search as a new candidate path; the escalation hole closed (`related` escalates beside `distinct`, entity-path pairs qualify unscored, `contradicted` rows are candidates so a revision can supersede a finding's party); numeric and unit reasoning runs deterministically before the judge (`ingestion/domain/quantity.ts`: both decimal conventions, dimension conversion, ranges, tolerances, precision; a same-slot conflict needs no model call, everything else reaches the judge with a `PARSED QUANTITIES` block); the **checked-pair ledger** (`checked_pair`) persists every verdict with prompt version and model configuration, so an unchanged pair is never re-judged and the nightly flip-flop is structurally gone; thresholds are a function of the active embedding model, failing loudly on an unknown one; the supersession direction guard tie-breaks equal event times on recording order and losers close at event time, never merge time; timing misses are closed by the delayed per-source `reconcile.repair` job and the confirm-time eligibility re-pair, and every finding records `detected_by` and its detection time. The **findings lifecycle** (issue E, `memory_relation_event`): open, resolved by user, resolved by revision (conservative, cause recorded with the `source_revision` link where one exists), kept open with the reason when ambiguous, following the successor when the conflict persists, and REOPENING the original finding when a revision reintroduces it. Resolved findings leave every current surface and stay queryable with history. The original gap list:
- **Entity matching beyond byte-equality:** aliases, typos, and cross-language subjects (today "Adriatic Foods" and "Jadranske hrane" can never pair). Anchoring from 4.2 feeds this directly.
- **The 0.80 to 0.93 similarity band and the escalation hole:** a `related` dedup verdict never escalates, so high-similarity paraphrased conflicts are structurally invisible.
- **Numeric and unit reasoning:** none exists today; a judge prompted "in doubt, compatible" will not catch 3.2 mm versus 3.4 mm.
- **Persist `compatible` verdicts in a checked-pair ledger,** so borderline pairs are not re-judged nightly and cannot flip to contradicted days later from model variance alone. This also removes a recurring nightly token cost.
- **Supersession interval arithmetic:** fix the failing case where both sides carry explicit validity and the judgment still misses.
- **Per-embedding-model threshold calibration:** the 0.80 and 0.93 constants silently mean something different under a different embedding model.
- **Budget and pairing policy** so crowded topics and simultaneous ingestion cannot hide the true conflict; contradictions found later are surfaced with their detection date, since the report must state when a finding appeared.

*Inherited from 5.3:* the `source_revision` link (auto, confirmed or manual) is a **document-level prior** the pairing policy can consume: facts whose sources are linked revisions of one another are the highest-value pairs to check for supersession rather than contradiction, and the link's basis (anchored revision fields, structural similarity) plus the owner's confirm/reject audit trail say WHY the two documents are held to be versions. Contradictions whose two sides sit across a confirmed revision link should resolve toward supersession by default; today reconciliation reaches that verdict only from fact text and validity intervals.

**6.2 Report generator.** **DELIVERED 2026-08-07** (migration 0049, `feature/findings-report`, published contract [`docs/findings-report-schema/`](findings-report-schema/README.md), decision record [`docs/features/report.md`](features/report.md)). A new `reports/` module owns the `findings_report` run ledger; generation is a worker job with visible progress that re-reads everything through the owners' Principal-gated interfaces (the sources/ shape, worker-side). Scope is first-class (corpus, import, source set, date range) and stated on the report; the delta against the previous run over the same scope is computed from the 6.1 event log (resolved with the resolving revision, still open, newly appeared, reopened), and a first run says so instead of showing zeros. One payload feeds both formats: the JSON is the SIGNED record (receipt-convention ed25519 over the canonical payload hash, schema published and drift-guarded, fictional sample included, verifiable end to end with jq + openssl and CI-proven), the PDF a deterministic dependency-free rendering (hand-rolled writer, vendored DejaVu faces with ToUnicode so evidence survives copy/paste, the brand logo drawn as vectors from the provided file, black-and-white-safe evidence panels, TOC, page numbers, report-id footer, visible truncation with the JSON keeping full text). Coverage and limits precede the findings (unreadable and truncated sources with reasons, gate refusals, OCR/vision-recovered pages flagged per finding, stated bounds, sensitive facts withheld and counted); trust scores are looked up by exact configuration id from the bundled artifacts and their absence is stated, never borrowed. Findings group by subject entity (a conflict belongs to a question, not either document). Deletion coverage repeats the passport pattern exactly (`findings_reports_expired` on the receipt, both object keys erased and swept, download refused with the reason, SEC-8 race guard); lifecycle audited (requested, ready, downloaded, expired); artifacts retained 24 hours like passports, run rows permanent for the delta. The original one-paragraph spec stands below.

*Original spec:* A signed, printable artifact from a findings run. Header: instance, corpus, date range, model configuration, and the trust scores for that configuration. Then **per contradiction**: both claims, both verbatim source spans, document plus revision plus location, detection date, resolution status. Then **superseded facts with their chains**. Then the **suppressed-fact log summary** from 3.3. Formats: **PDF** for the auditor, **JSON** for machines. Reuses the passport assembler pattern and receipt signing, so the report is verifiable the same way receipts are.

*Inherited from 5.3:* "document plus revision" is readable today: the anchoring context's revision field plus the `source_revision` chain (followed predecessor-to-successor from `SourceRevisionStore.forSource`) give the report the document's version lineage with each link's basis and who decided it; an `import_run` with its per-file outcomes is a ready-made **corpus statement** for the report header (what was imported, when, what was skipped or unreadable, every count real), and the 5.2 catalog reads (`sourceFactStatsForRefs`, `openContradictionCountsForSources`, `readOutcomesForKeys`, `refusalsForSources`) are the same grouped queries a findings run needs, already shaped for it.

**6.3 Ambiguity detection and fan-out answers.** **DELIVERED 2026-08-07** (migration 0050, `feature/ambiguity-fanout`, ambiguity config v1, decision record [`docs/features/ambiguity.md`](features/ambiguity.md)). The decision is a pure function in `retrieval/ambiguity.ts` over the post-fusion distribution: clusters keyed by the alias-canonical folded anchored subject (the 6.1 fold plus the owner's `entity_alias` set, so one real subject under two names never fans out), cluster relevance = best member vector similarity (carried through fusion for the first time; rank-derived RRF cannot carry an absolute floor), cluster comparability = ratio of max member fused scores (never a sum: volume must not beat relevance). Evaluated in order: a query-named subject wins (also how a fan-out's follow-up resolves, deterministically, via the stored offer), then the relevance floor (below it nothing counts, including for fan-out lines), then comparability. Thresholds live in `retrieval/ambiguity-config.ts` per embedding model, failing loudly on an unknown one, calibrated live on `mistral-default` (relevant clusters measured 0.92 to 0.94, foreign 0.79 to 0.876; floor 0.90 in the gap; ratio 0.55 above the measured two-signal consensus gap of 0.49 and far below same-signal adjacency at 0.97). The fan-out and the silence preamble are fully **server-authored** (localized, anchor language, real `{{cite}}` tokens, verdict words for non-active facts, capped at 4 lines with an honest "N more"), so the whole feature adds **zero model calls** and `answer/v0008` stands unchanged; the silent branch splits by question class (knowledge gets the preamble then marked `[U]` general knowledge with sub-floor facts withheld; personal keeps `nothingOnRecord`). Every grounded answer records its decision on `chat_message.ambiguity` (content-bearing, cleared by the answer redaction cascade), which is both the diagnosis path and the follow-up resolver's input. Gated: the three plan-named cases in both languages plus follow-up, weak-dominance and cap edges as deterministic chat rule checks, branch regression assertions on ordinary cases, and the harness prints the branch distribution (fan-out rate) over every run. The original paragraph stands as specified: after fusion, the answer path computes the score distribution across anchored-entity clusters and acts deterministically, with no user configuration and one behavior always: a **single dominant cluster** answers normally with citations; **no cluster above the relevance floor** means the corpus is silent, and the answer says so explicitly, then continues with general model knowledge under a clear "not from your sources" banner; **several comparable clusters** with distinct subject entities produce a **fan-out answer**, one line per cluster with fact, citation, and validation verdict where the question implies one, ending with "which did you mean?". Never a silent guess, never a bare clarifying question. Detection is deterministic (post-RRF score distribution over entity groups, thresholds versioned like the reconcile config), costs no extra model call, and depends on 4.2 for clean clusters. Gated by three golden cases: a context-resolved fragment that must pick the thread's subject; a cold ambiguous value that must fan across exactly the domains holding related facts; and a silent-corpus question that must banner general knowledge without fabricating a source.

**6.4 Eval gates, second wave.** **DELIVERED 2026-08-08** (`feature/vertical-eval-gates`, gates v3, trust-score schema **1.2**, corpus [`project/eval/vertical/`](../project/eval/vertical/README.md), diagnostic report [`docs/eval/vertical-corpus-diagnostic.md`](eval/vertical-corpus-diagnostic.md)). **No application behaviour changed**: the diff is evaluation, corpus, configuration and documentation only.

A **vertical corpus** of **44 labelled cases** (20 extraction, 24 reconciliation pairs) over **13 real, public documents**, every one sourced whole with its URL, publisher, licence, retrieval date and SHA-256 recorded in `documents.json`, the bytes fetched rather than committed (`fetch.mjs`) and the committed excerpts verbatim reader output. Four document types, chosen for the difficulty they actually carry: an EU regulation and the act amending it (EN and HR), two revisions of NIST SP 800-171 with four requirements withdrawn between them, the RP2040 and RP2350 datasheets whose XOSC sections are word-for-word identical apart from the numbers, two public tender notices (an EU transformer procurement and a Croatian mask procurement whose three lots differ only by product class), two Croatian construction regulations one of which amends the other, and a 1987 NBS scan. Croatian-origin material is labelled as such and distinguished from the Croatian authentic language versions of EU acts, which are translations and say so. **The labelling rules were written before the first label** ([`LABELLING.md`](../project/eval/vertical/LABELLING.md)): what counts as a fact worth extracting from a specification, what separates a genuine contradiction from a legitimate difference of subject, slot, condition or precision, when two documents about different models must not be paired, and the standing rule that an ambiguous case is labelled conservatively and flagged rather than silently decided. Every case is traceable to its document, page range and reasoning.

**Reported and gated as its own corpus, never averaged into the core one**: a `vertical` block in `gates.json` with the same two layers plus the union rule, both zero-tolerance gates counted separately, and schema 1.2's `corpora` array carrying both corpora side by side in the published artifact. **The vertical numbers are lower and they are published anyway**, which is the governing rule from 3.4 doing its job. Extraction **recall** on documents is strong (91.9% aggregate) and extraction **precision** is not (49.8%), almost entirely because real documents hand back page furniture, notice identifiers, eSender postcodes and registry UUIDs that are true and worthless. The reconciliation arm is where the document claim is weakest: **supersession across document revisions scored 0 of 5** and **cross-language contradiction detection 0 of 2**, with the failures landing in the safe direction (a supersession surfaced as a contradiction for a human) rather than the dangerous one.

**Numeric/unit and cross-language contradiction pairs** now exist over real spans, and two shapes the plan named have **no real instance in this corpus**, recorded rather than invented: a differing-unit same-dimension conflict about one subject, and a plain non-supersession contradiction in English. Both cross-language contradictions were **found, not written**, by comparing every number on every page of the two authentic language versions of Regulation (EU) 2017/745. **Authority-ranking cases** are authored and **PENDING** in `project/eval/vertical/authority/`, outside the directory the harness loads, because the behaviour has not shipped and the governing rule forbids a gate the project is currently failing; two of the four carry a placeholder side and say so in the file. **Anchoring lands as gates** through five `subject_entity` declarations under the zero-tolerance `subject_mismatches` gate; the Croatian declarations were removed after the first live run returned null or the document title, with the removal and the reason recorded in each case and in the changelog. **Ambiguity and fan-out** stay gated where 6.3 put them, in the chat suite's deterministic branch checks.

**The first real-document ingestion is a deliverable in its own right** and seeds later versions: a malformed structured output makes a large document cost three full passes and two of five large documents finished holding nothing; the anchor call is abandoned on the pages with the most subjects because its subject list is capped; the document default outranks the section subject on almost every fact; the quantity parser does not know `m²`, `ppm`, `Ω`, `pF`, `MVA` or the masculine-ordinal degree sign a real datasheet prints, and reads `1,5 kg/m²` as a mass; a whole datasheet exceeds the text cap and a 141-page scan exceeds the 30-second parse timeout; and Croatian regulations are published as HTML, which the reader seam does not cover. Each is written up with its evidence in the diagnostic report.

*Original spec:* A **vertical golden set**: 30 to 50 labeled cases from real regulatory or requirements documents, because the engine is proven on notes and emails while the claim being sold is about the buyer's document type. **Numeric/unit and cross-language contradiction pairs** (zero exist today). **Authority-ranking cases** once that ships. Anchoring and fan-out cases from 4.2 and 6.3 land here as gates.

---

## 7. V2.4 Operate: configuration, cost, and observability

| # | Item | Priority | Difficulty |
|---|---|---|---|
| 7.1 | **Provider configuration in the database + admin UI**: **DELIVERED** in two halves, configuration 2026-08-10 (migration 0052) and the managed reindex 2026-08-10 (migration 0053) | P0 | M |
| 7.2 | **Token accounting and the counterfactual** | P1 | M |
| 7.3 | **Cost reduction programme** | P1 | M |
| 7.4 | **Observability** | P1 | L |
| 7.5 | **Airgap hardening** | P2 | S to M |

**7.1 Provider configuration in the database.** Move model and provider configuration out of `.env` into the database with **encrypted keys**; the master key stays in `.env`; existing `.env` values seed the database on first run. An **admin UI page** manages providers. The **chat model becomes user-switchable** in the UI. Extraction and verification models stay **admin-only**, each shown with **its eval trust scores**, and untested combinations flagged as **"not evaluated."** `.env` keeps only bootstrap: database credentials, master key, instance configuration. This is also the natural break point for the legacy environment-variable expansion kept for v1 parity.

**Delivered (configuration half), 2026-08-10.** Migration 0052, the new `providers`
module, six tables. Providers are **records** an admin manages: a label they choose, a
type (Mistral, OpenAI, Anthropic, **Self-hosted** = any OpenAI-compatible endpoint),
an endpoint where the type needs one, a key **encrypted at rest** with AES-256-GCM
under `COGETO_MASTER_KEY`, which stays in the environment because a key that guards a
database cannot live inside it. Several providers of the same type are ordinary, so
the label is the identity; a reserved fifth stored type, `ollama`, exists only so an
instance already on the local runtime keeps its adapter and its configuration id
exactly. **A saved key never comes back out**: the sealed column is selected in one
function, and `key-confinement.spec.ts` asserts that against the source rather than by
convention. Four independent assignments (pipeline, answer, embeddings, vision), with
vision optionally unassigned. **Discovery offers, manual entry always works**, because
a proxied deployment legitimately serves models its `/models` route does not
advertise; **validation is a probe of the tier's real job**, never a pattern match on
a name, classified into unreachable / key rejected / model not served / no embeddings
route / image refused / timed out. The configuration id derivation is untouched, every
change is recorded with the id it produced, and the assignment page shows the published
trust score for the exact configuration in force or says **"not evaluated"** in words.
The **answer** tier is user-switchable among the models an admin enabled, as an opaque
option id so call sites still name a tier; pipeline, embeddings and vision stay
admin-only. Changes take effect **without a restart**: one live configuration object per
process, mutated in place, and the worker polls the version column. **Seeding is
atomic and happens once**: the reference deployment (one Caddy vhost in front of two
llama.cpp processes at `host.docker.internal:9000/v1`, no key) comes across as one
Self-hosted provider and four assignments resolving to the same bindings and the same
configuration id, and after the seed the environment's model variables are **ignored**
rather than merged. `.env` is reduced to bootstrap.

**Refused on purpose: an embeddings change.** Every stored vector was produced by the
current model, so a change needs the index rebuilt, and the managed rebuild is the
second half. The interface explains that and names the operator command as the interim
path; the refusal is server-side, so it holds for any caller.

**Delivered (managed reindex half), 2026-08-10.** Migration 0053: the vector index
gets durable state (`embedding_index_state`, memory-owned, one row: active collection,
active dimension, and the rebuild in flight), and changing the embeddings model
becomes a safe in-application operation. The interface runs a two-step plan/confirm
flow that probes the candidate with a real embedding (yielding its TRUE dimension),
then states the corpus size, the chars/4 token estimate the meter will actually
charge, a probed duration estimate, the spend, and the serving behaviour, before
anything is saved. The pending model is recorded beside the active one; a
`memory.reindex_advance` worker job (the `import.advance` shape) re-embeds the corpus
from Postgres into a NEW collection while the old one keeps serving untouched, with
resume-by-presence across restarts, metered and attributed spend, budget exhaustion
pausing rather than bypassing, and cancel always available. The switch is ONE
transaction under an embedding-write lock every stamped-vector writer shares: final
catch-up, gate-payload resync, orphan sweep, count verification, per-row stamp,
assignment flip through a port, state flip; a crash rolls it all back to a running
rebuild over a serving index. Gate parity in the new collection is asserted by test,
payload writes and deletions dual-apply during the rebuild, and the nightly sweep
drops stray rebuild collections. The boot guard stays as a net with an actionable
message naming `cogeto reindex`, the new operator subcommand that shares the same
engine (flagless in-place repair, or `--provider LABEL --model M` for an offline
managed switch via `compose run`, which works while the services crash-loop). The
overriding property, verified end to end: **no interface action can render the
instance unstartable**, and at every point there is a coherent active configuration
whose index matches it. Details:
[`docs/features/models.md`](features/models.md).

**The legacy environment expansion stays, for one release.** It is not dead: it is
exactly what a v1-era instance's SEED depends on, and this is the upgrade where those
values must still be read correctly. It becomes dead the moment every instance has
seeded, and is removed then. Details:
[`docs/features/models.md`](features/models.md),
[`docs/operations/upgrade-notes.md`](operations/upgrade-notes.md).

**7.2 Token accounting and the counterfactual.** Per-operation token accounting (the budget infrastructure exists; this surfaces it) rolled up per user, per instance, and per period, visible in an admin view, broken down by task family (extraction, verification, reconciliation, answering, research, vision). Plus the **counterfactual comparison**: what the same work would have cost without Cogeto, defined as a documented, checkable baseline (for example, feeding the full text of every source that a set of answers cited, or the full corpus for corpus-wide questions) rather than an invented multiplier. Shown as an estimate with the methodology stated and linkable, per the project's own honesty rules. Done right, this is both a cost tool and a sales artifact; done loosely it is exactly the kind of claim this brand cannot afford.

**7.3 Cost reduction programme.** Concrete levers, most of which fall out of work already planned: the checked-pair ledger stops re-judging compatible pairs nightly (6.1); anchoring reduces ambiguous re-extraction (4.2); the per-source extraction gate stops corpus flooding (4.3); content-hash dedup avoids re-ingesting identical documents (5.3); batch verification and pipeline-tier routing already exist and are audited for gaps; cached evals cut CI spend (3.4); local models remove the highest-volume external call. Each lever is measured against the accounting from 7.2 rather than assumed.

**7.4 Observability.** Today there is health, structured logs, and a jobs view, and **no metrics, no tracing, no alerting**. Add metrics (counters and latency series per stage, per provider, per job type), **correlation IDs** so one request can be followed from HTTP through job to model call, an alerting hook for degraded health and integrity alerts, log shipping and retention guidance, and historical health. Complete the **read-audit coverage** started in 3.7 and give the audit log a retention policy and an export path.

**7.5 Airgap hardening.** Offline is already genuinely working (local model preset, local embeddings, in-instance storage, no telemetry, one CI-enforced egress seam, and a recorded offline eval of 24 of 27 chat cases). Close the remaining gaps: an **offline image bundle** story (`docker save`-style) since image pulls are the only remaining network need, and move the three services still built from source rather than pulled as images.

---

## 8. V2.5: Connect and organize

| # | Item | Priority | Difficulty |
|---|---|---|---|
| 8.1 | **Connector platform**: **delivered** | P0 | L |
| 8.2 | **First external connector (by demand)** | P1 | M each |
| 8.3 | **Projects as workspaces** | P1 | M |

**8.1 Connector platform.** All greenfield, per the audit: **credential storage** (token table, secret encryption, refresh loop, kept inside the identity seam), **sync and cursor state** (delta and history tokens: promised in the README, never implemented), **outbound rate limiting** (no token bucket or Retry-After handling exists), a **webhook ingress framework** (HMAC, replay protection, dedup, subscription renewal), and **natural-key deduplication** so a polling connector re-returning the same item does not cost N extractions (no remote-id uniqueness exists anywhere today). The `source_type` registry from 3.6 and the extraction gate from 4.3 are prerequisites.

**Delivered** (migration 0054, the new `connectors` module plus identity's `connector_credential`). The decision record, frozen before code, is [`docs/features/connectors.md`](features/connectors.md); the guide a future connector follows is [`docs/features/connector-authoring.md`](features/connector-authoring.md). No external service was integrated, by design: the platform is proved by a reference connector that exists only in tests, implementing paging, cursors, edits, deletions, duplicate webhook deliveries, expiring credentials, rate limiting with Retry-After and webhook signatures, and every future connector validates against that harness before it ships. The two expensive-failure properties are named tests: an interrupted and resumed sync re-extracts nothing, and a full re-sync over unchanged upstream data costs zero model calls. Existing connectors were left untouched with the reasons recorded: nothing regressed.

**8.2 First external connector.** Google, Microsoft, Slack, Teams, or Jira: **one, chosen by partner demand, not five.** Observed sources carry third-party content at volume, which is why 4.3 ships first. Each subsequent connector is its own unit of work with its own eval cases.

**8.3 Projects as workspaces.** Confirmed shape: **projects organize conversations, files, and research runs; memory stays one shared pool.** No third gate dimension on memory, no per-project memory isolation (the audit priced that at 55 to 70 files, a full reindex, two broken published contracts, and an inversion of the existing continuity decision). Per-user private context continues to be handled by the existing scope model. Optionally, a retrieval **filter lens** ("answer from this project's sources") that narrows results without changing the gate. The public wording is fixed and must not drift: *projects organize your work and keep your files and context private; memory stays one connected mind.*

---

## 9. V2.6 and later: platform, gated by evidence

| # | Item | Priority | Difficulty |
|---|---|---|---|
| 9.1 | **Agent governance productization** | P2 | L |
| 9.2 | **Evaluation control plane** | P2 | L |
| 9.3 | **Plugin extension platform** | P2 | XL |
| 9.4 | **Enterprise identity** | deferred | L |

**9.1 Agent governance.** The spine exists (approval state machine, append-only audit log, skills with visible step logs). This is productizing it into a coherent, visible governance layer: policy over which actions require approval, per-action audit views, and the run inspector as a first-class surface. Dual-use for solo and enterprise.

**9.2 Evaluation control plane.** Elevate the harness from a CI gate into an operable surface: run evaluations on demand, compare configurations, track drift over time, and publish from one place. The per-configuration trust-score schema already supports this.

**9.3 Plugin extension platform.** Deliberately last. A plugin system is a security surface, a support surface, and an architectural commitment; it lands on the clean boundaries from 3.6 and the connector platform from 8.1, or it does not land. Scope when reached: extension points (source readers, extractors, actions), a sandboxing and permission model, versioned contracts, and a review posture.

**9.4 Enterprise identity.** SAML, SCIM, LDAP, a richer role model beyond the single admin role, session management. Confirmed **not needed now**; revisit only when a paying enterprise requires it.

---

## 10. Traceability: every confirmed item, mapped

| Confirmed item | Version | Section |
|---|---|---|
| Remove tasks fully, backtraced, no leftovers | V2.0: **delivered** | 3.1 |
| Reminders removed | V2.0: **delivered** | 3.2 |
| Cogeto resolves reviews itself (unsupported/partial automatic) | V2.0: **delivered** | 3.3 |
| Suppressed-fact log (feeds the report) | V2.0: **delivered** | 3.3 |
| Contradictions surfaced, not queued in Review | V2.0 principle, V2.3 depth | 2, 6.1 |
| Enhanced trust score on contradictions, better metrics | V2.0: **delivered**, V2.3 deepens | 3.4, 6.4 |
| Contradiction precision gated and published | V2.0: **delivered** | 3.4 |
| Supersedes gated | V2.0: **delivered** | 3.4 |
| Per-language floors (hr cannot hide in aggregate) | V2.0: **delivered** | 3.4 |
| Query-rewrite eval cases | V2.0: **delivered** | 3.4 |
| Evals on PRs via cached responses | V2.0: **delivered** for the golden-set suite; chat suite blocked on a retrieval ordering tiebreak (follow-up) | 3.4 |
| i18n user-level, en/hr/de/fr, English fallback, keys in sync, library | V2.0: **delivered** (translations are a separate task) | 3.5 |
| Modularization / cleaner code / less coupling | V2.0 | 3.6 |
| `source_type` enum to registry | V2.0 | 3.6 |
| Audit read coverage incl. passport export | V2.0 | 3.7 |
| Read every document: XLSX/CSV | V2.1 | 4.1 |
| Read every document: OCR for scans (local, en/hr) | V2.1 | 4.1 |
| Read every document: local vision model for hard cases | V2.1 | 4.1 |
| Honest "no readable text" labelling | V2.1 | 4.1 |
| Source-context anchoring (subject, class, revision; editable; re-anchor as supersession) | V2.1 | 4.2 |
| Per-source extraction gate | V2.1 | 4.3 |
| Remove standalone note field and upload from Memories | V2.2 | 5.1 |
| Chat capture only; paperclip file attach; inline "added: N facts, M contradictions" | V2.2 | 5.1 |
| "Don't remember this file" transient toggle | V2.2 | 5.1 |
| Memories becomes Sources: list / source detail / fact detail | V2.2 | 5.2 |
| Exact source span shown per fact | V2.2 | 5.2 |
| Flat memory list demoted to filtered search | V2.2 | 5.2 |
| Bulk import (folder/ZIP/S3, manifest, hash dedup, caps, progress, summary) | V2.2 | 5.3 |
| Contradiction coverage: aliases, typos, cross-language entities | V2.3 | 6.1 |
| Contradiction coverage: 0.80 to 0.93 band and escalation hole | V2.3 | 6.1 |
| Contradiction coverage: numeric and unit conflicts | V2.3 | 6.1 |
| Persist compatible verdicts (checked-pair ledger, stops nightly flip-flop) | V2.3 | 6.1 |
| Supersession interval arithmetic fix | V2.3 | 6.1 |
| Per-embedding-model threshold calibration | V2.3 | 6.1 |
| Timing misses (simultaneous uploads, per-fact budget, uncertain exclusion) | V2.3 | 6.1 |
| Report generator (signed, PDF + JSON, contradictions, superseded, suppressed) | V2.3 | 6.2 |
| Ambiguity detection and fan-out answers | V2.3 | 6.3 |
| Vertical golden set (30 to 50 real document cases) | V2.3 | 6.4 |
| Numeric and cross-language contradiction pairs | V2.3 | 6.4 |
| Authority-ranking cases | V2.3 | 6.4 |
| Provider config in database, encrypted keys, master key in .env, seeded | V2.4 | 7.1 |
| Admin UI for providers; user-switchable chat model; admin-only extraction/verification with trust scores; untested flagged | V2.4 | 7.1 |
| Token counting | V2.4 | 7.2 |
| Counterfactual comparison (cost without Cogeto) | V2.4 | 7.2 |
| Cost reduction / fewer tokens | V2.4 (levers throughout) | 7.3 |
| Observability (metrics, tracing, alerting) | V2.4 | 7.4 |
| Audit logging completeness and retention | V2.0 + V2.4 | 3.7, 7.4 |
| Airgap support hardening | V2.4 | 7.5 |
| Connectors (Google, Microsoft, Jira, Slack, Teams) | V2.5 | 8.1, 8.2 |
| Projects (workspaces, not memory isolation) | V2.5 | 8.3 |
| Enterprise agent governance | V2.6+ | 9.1 |
| Evaluation control plane | V2.6+ | 9.2 |
| Plugin extension | V2.6+ | 9.3 |
| Enterprise identity | deferred | 9.4 |
| Verification before storage | already built; sub-reasons split in V2.0 | 3.3 |
| Temporal validity | already built; supersession fixes in V2.3 | 6.1 |

---

## 11. Sequence and rationale

**V2.0 → V2.1 → V2.2 → V2.3** is fixed. Removal and truth first because the codebase gets smaller and the published numbers get honest before anything is built on them; reading and anchoring next because everything downstream depends on documents being readable and facts knowing their subject; Sources and bulk import next because they are how a corpus gets in and gets proven; findings last in the wedge because contradictions and the report are the payoff and need all three predecessors.

**V2.4 and V2.5** can reorder against evidence: if a design partner needs a connector before cost visibility, swap them. **V2.6 and later** are gated by paying demand, not schedule.

One standing rule for the whole cycle, unchanged from v1: every capability ships with its eval cases, gates ratchet up and never silently down, and the published trust score tells the truth including the parts that are not flattering.
