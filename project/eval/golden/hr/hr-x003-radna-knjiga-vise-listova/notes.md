# hr-x003: radna knjiga s više listova

The Croatian twin of en-x003. The sheet names are Croatian (`Dobavljači`,
`Obnove`), which is the part worth having twice: a sheet name is document
content, it reaches the extractor inside the context line, and it is stored on
the read report, which is why that table is in the deletion cascade.
