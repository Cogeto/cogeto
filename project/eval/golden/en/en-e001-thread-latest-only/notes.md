Thread-aware extraction (O4): the new message commits to sending the contract by
Thursday. The quoted prior message is stripped before extraction (it is already
its own source), so only the new commitment is remembered.
