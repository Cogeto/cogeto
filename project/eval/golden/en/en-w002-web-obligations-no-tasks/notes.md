Observed-obligation case: a fetched web page dense with obligation-shaped
language. The obligations belong to the world (Adriatic Foods, Marko Novak),
not the user — they must all become memories (extracted, verified, citable)
and must not be attributed to the user.

Originally a P6.5 derivation trap (decision 0054, `expected_tasks: 0`). The
task subsystem was removed in 2.0 (decision 0060), so the task assertion is
gone; the case id is kept so published scores stay comparable.
