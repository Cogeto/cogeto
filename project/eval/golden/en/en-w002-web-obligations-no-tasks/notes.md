Derivation trap (P6.5, decision 0054): a fetched web page dense with
obligation-shaped language. The obligations belong to the world (Adriatic
Foods, Marko Novak), not the user — they must all become memories (extracted,
verified, citable) and derive ZERO tasks. `expected_tasks: 0` is a hard
assertion checked against the tasks module's real first-person predicate; it
fails the gate if web sources ever derive tasks again.
