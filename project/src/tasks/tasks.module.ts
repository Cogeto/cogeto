import { Module } from '@nestjs/common';

/**
 * tasks — memory turned into action (scope §4.7): todos, open loops, reminders,
 * digests. Reads memory through its public interface; never mutates it.
 * Shell module until the tasks slice.
 */
@Module({})
export class TasksModule {}
