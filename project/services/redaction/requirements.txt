# Redaction sidecar (spec §12.2). CPU-only; pinned for reproducible fleet builds.
#
# This is the human-readable TOP-LEVEL input. The Dockerfile does NOT install from
# here — it installs from the fully hash-locked requirements.lock (compiled from
# this file with `pip-compile --generate-hashes --allow-unsafe` in python:3.12,
# SEC-12). When you change a pin below, regenerate the lock (docs/operations/
# image-pins.md).
fastapi==0.141.1
uvicorn[standard]==0.52.1
presidio-analyzer==2.2.364
presidio-anonymizer==2.2.364
# NOT 3.8.14: presidio-analyzer pins spacy !=3.8.14 (3.8.15 satisfies it).
spacy==3.8.15
# The spaCy NER model (en_core_web_lg) is installed in the Dockerfile from a
# version-pinned release wheel, not pinned here.
