# Redaction sidecar (Addendum B.8). CPU-only; pinned for reproducible fleet builds.
#
# This is the human-readable TOP-LEVEL input. The Dockerfile does NOT install from
# here — it installs from the fully hash-locked requirements.lock (compiled from
# this file with `pip-compile --generate-hashes --allow-unsafe` in python:3.12,
# SEC-12). When you change a pin below, regenerate the lock (docs/operations/
# image-pins.md).
fastapi==0.111.0
uvicorn[standard]==0.30.1
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.363
spacy==3.7.5
# The spaCy NER model (en_core_web_lg) is installed in the Dockerfile via
# `python -m spacy download`, not pinned here.
