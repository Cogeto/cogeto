; In-process per-remote-host connection concurrency limit (GAP-1). No Redis —
; see plugins/cogeto_connlimit.js. The real abuse throttle is the app-side
; per-sender intake rate cap (SEC-2, COGETO_MAIL_INTAKE_MAX_PER_SENDER).
[main]
max=3
