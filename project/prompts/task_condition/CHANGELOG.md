# task_condition — changelog

- **v0001** (2026-07-05, F3-B): initial condition-satisfaction prompt
  (decision 0013 ruling 3). Verdict satisfied | not_satisfied | unrelated with
  one-sentence reason; cost table in-prompt ("a wrong satisfied un-blocks too
  early; doubt resolves to not_satisfied"; the condition must be met AS
  STATED); two contrast examples (exact prerequisite vs scheduled-not-given).
  Baseline scored by the task pair cases (docs/eval/history.md).
