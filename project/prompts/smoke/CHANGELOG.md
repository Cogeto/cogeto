# smoke — changelog

- **v0001** (2026-07-02, S1-B): initial smoke prompt for the gateway round-trip check
  (`npm run gateway:smoke`). Not a production extraction prompt; the real extraction
  family arrives with the Notes slice and the eval harness (§B.4).
